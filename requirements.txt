# Versions pinned to match the environment this project was run in.
matplotlib==3.10.7
numpy==2.3.5
pandas==2.3.3
requests==2.32.5
statsmodels==0.14.5

# These were installed ad hoc in the original environment and were never
# pinned, so they are left unconstrained. Pin them once you confirm versions.
groq
python-dotenv
